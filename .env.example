# Copy this file to ".env" and fill in your real key.
# Get one by following the steps in README.md → "Google Maps setup".
# Never commit the real ".env" file — it's already in .gitignore.
VITE_GOOGLE_MAPS_API_KEY=
