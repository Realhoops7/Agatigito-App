node_modules/
dist/
.env
.env.local

# Capacitor native projects — generated by `npx cap add android`.
# Commit these too once they exist (they're not build output, they're your
# Android project), but they don't exist yet in this scaffold, so ignoring
# for now keeps a clean first commit. Remove this line once you've run
# `npx cap add android` and are ready to commit the android/ folder.
android/

# Editor/OS noise
.DS_Store
.vscode/
.idea/
*.log
